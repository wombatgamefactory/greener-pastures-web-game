dist/
node_modules/
.scratch/
.scratchprobe/
.probe/
captures/
reports/
package-lock.json

# Generated by tools/extract_cards.py, which owns its own formatting. Letting
# Prettier reformat it would make every re-extract dirty the working tree.
packages/data/data/cards.json

# The replay fixtures, same argument and a sharper one (2026-09-12). They are
# written by the harness, not by hand, and they are the proof that a rules change
# replays byte-identically. Prettier reflowed all nine on 12/09/2026: the DATA
# survived (fixtures.test.ts parses rather than byte-compares, so nothing broke)
# but a regenerate would have fought the reformat every time.
packages/sim/fixtures/
